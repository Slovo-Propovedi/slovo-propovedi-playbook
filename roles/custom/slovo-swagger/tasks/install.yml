---

- name: Ensure slovo-swagger paths exist
  ansible.builtin.file:
    path: "{{ item.path }}"
    state: directory
    mode: '0750'
    owner: "{{ slovo_user_name }}"
    group: "{{ slovo_group_name }}"
  with_items:
    - path: "{{ slovo_swagger_base_path }}"
      when: true
    - path: "{{ slovo_swagger_container_src_path }}"
      when: "{{ slovo_swagger_container_image_self_build }}"
  when: item.when | bool

- name: Ensure slovo-swagger labels file installed
  ansible.builtin.template:
    src: "{{ role_path }}/templates/labels.j2"
    dest: "{{ slovo_swagger_base_path }}/labels"
    mode: '0640'
    owner: "{{ slovo_user_name }}"
    group: "{{ slovo_group_name }}"
  register: slovo_swagger_labels_file_changed

- when: slovo_swagger_container_image_self_build | bool
  block:
    # A checkout owned by a different user (a uid change, an earlier clone by another user, etc.) would make the git task below fail on ownership or permissions.
    - name: Ensure slovo-swagger repository ownership is correct on self-build
      ansible.builtin.file:
        path: "{{ slovo_swagger_container_src_path }}"
        state: directory
        owner: "{{ slovo_user_name }}"
        group: "{{ slovo_group_name }}"
        recurse: true

    - name: Check if slovo-swagger repository is a valid git checkout
      ansible.builtin.stat:
        path: "{{ slovo_swagger_container_src_path }}/.git"
      register: slovo_swagger_git_check

    - name: Remove broken slovo-swagger repository if not a valid git checkout
      ansible.builtin.file:
        path: "{{ slovo_swagger_container_src_path }}"
        state: absent
      when: not slovo_swagger_git_check.stat.exists

    - name: Ensure git trusts the slovo-swagger repository directory
      ansible.builtin.command:
        cmd: "git config --global --add safe.directory {{ slovo_swagger_container_src_path }}"
      changed_when: false

    - name: Ensure slovo-swagger repository is present on self-build
      ansible.builtin.git:
        repo: "{{ slovo_swagger_container_image_self_build_repo }}"
        version: "{{ slovo_swagger_container_image_self_build_repo_version }}"
        dest: "{{ slovo_swagger_container_src_path }}"
        force: "yes"
      register: slovo_swagger_git_pull_changed

    # The git clone above runs as the ansible user (root), so the cloned files
    # end up owned by root. Restore ownership to the slovo user afterwards.
    - name: Ensure slovo-swagger repository ownership is correct after git clone on self-build
      ansible.builtin.file:
        path: "{{ slovo_swagger_container_src_path }}"
        state: directory
        owner: "{{ slovo_user_name }}"
        group: "{{ slovo_group_name }}"
        recurse: true

    # The swagger repository's Dockerfile is at the repository ROOT (not in a subdirectory).
    - name: Ensure slovo-swagger container image is built
      ansible.builtin.command:
        cmd: |-
          {{ devture_systemd_docker_base_host_command_docker }} buildx build
          --builder={{ slovo_buildx_builder_name }}
          --load
          --tag={{ slovo_swagger_container_image }}
          --file={{ slovo_swagger_container_src_path }}/Dockerfile
          {{ slovo_swagger_container_src_path }}
      when: slovo_swagger_git_pull_changed.changed
      register: slovo_swagger_docker_image_build_changed

- name: Ensure slovo-swagger container network is created
  when: slovo_swagger_container_network != 'host'
  community.general.docker_network:
    enable_ipv6: "{{ devture_systemd_docker_base_ipv6_enabled }}"
    name: "{{ slovo_swagger_container_network }}"
    driver: bridge
    driver_options: "{{ devture_systemd_docker_base_container_networks_driver_options }}"

- name: Ensure slovo-swagger.service installed
  ansible.builtin.template:
    src: "{{ role_path }}/templates/systemd/slovo-swagger.service.j2"
    dest: "{{ devture_systemd_docker_base_systemd_path }}/slovo-swagger.service"
    mode: '0644'
  register: slovo_swagger_systemd_unit_changed

- name: Determine whether slovo-swagger needs a restart
  ansible.builtin.set_fact:
    slovo_swagger_restart_necessary: >-
      {{
        slovo_swagger_labels_file_changed.changed | default(false)
        or slovo_swagger_git_pull_changed.changed | default(false)
        or slovo_swagger_docker_image_build_changed.changed | default(false)
        or slovo_swagger_systemd_unit_changed.changed | default(false)
      }}
