MINIO_ROOT_USER={{ slovo_minio_root_user }}
MINIO_ROOT_PASSWORD={{ slovo_minio_root_password }}
# VPS optimization: cap concurrent in-flight S3 API requests at 40 so MinIO's
# memory usage stays within its 768MB container limit under load.
MINIO_API_REQUESTS_MAX=40
