---

- name: Ensure slovo-backend paths exist
  ansible.builtin.file:
    path: "{{ item.path }}"
    state: directory
    mode: '0750'
    owner: "{{ slovo_user_name }}"
    group: "{{ slovo_group_name }}"
  with_items:
    - path: "{{ slovo_backend_base_path }}"
      when: true
    - path: "{{ slovo_backend_container_src_path }}"
      when: "{{ slovo_backend_container_image_self_build }}"
  when: item.when | bool

- name: Ensure slovo-backend env file installed
  ansible.builtin.template:
    src: "{{ role_path }}/templates/env.j2"
    dest: "{{ slovo_backend_base_path }}/env"
    mode: '0640'
    owner: "{{ slovo_user_name }}"
    group: "{{ slovo_group_name }}"
  register: slovo_backend_env_file_changed

- name: Ensure slovo-backend labels file installed
  ansible.builtin.template:
    src: "{{ role_path }}/templates/labels.j2"
    dest: "{{ slovo_backend_base_path }}/labels"
    mode: '0640'
    owner: "{{ slovo_user_name }}"
    group: "{{ slovo_group_name }}"
  register: slovo_backend_labels_file_changed

- when: slovo_backend_container_image_self_build | bool
  block:
    # A checkout owned by a different user (a uid change, an earlier clone by another user, etc.) would make the git task below fail on ownership or permissions.
    - name: Ensure slovo-backend repository ownership is correct on self-build
      ansible.builtin.file:
        path: "{{ slovo_backend_container_src_path }}"
        state: directory
        owner: "{{ slovo_user_name }}"
        group: "{{ slovo_group_name }}"
        recurse: true

    - name: Check if slovo-backend repository is a valid git checkout
      ansible.builtin.stat:
        path: "{{ slovo_backend_container_src_path }}/.git"
      register: slovo_backend_git_check

    - name: Remove broken slovo-backend repository if not a valid git checkout
      ansible.builtin.file:
        path: "{{ slovo_backend_container_src_path }}"
        state: absent
      when: not slovo_backend_git_check.stat.exists

    - name: Ensure git trusts the slovo-backend repository directory
      ansible.builtin.command:
        cmd: "git config --global --add safe.directory {{ slovo_backend_container_src_path }}"
      changed_when: false

    - name: Ensure slovo-backend repository is present on self-build
      ansible.builtin.git:
        repo: "{{ slovo_backend_container_image_self_build_repo }}"
        version: "{{ slovo_backend_container_image_self_build_repo_version }}"
        dest: "{{ slovo_backend_container_src_path }}"
        force: "yes"
      register: slovo_backend_git_pull_changed

    # The git clone above runs as the ansible user (root), so the cloned files
    # end up owned by root. Restore ownership to the slovo user afterwards.
    - name: Ensure slovo-backend repository ownership is correct after git clone on self-build
      ansible.builtin.file:
        path: "{{ slovo_backend_container_src_path }}"
        state: directory
        owner: "{{ slovo_user_name }}"
        group: "{{ slovo_group_name }}"
        recurse: true

    - name: Ensure slovo-backend container image is built
      ansible.builtin.command:
        cmd: |-
          {{ devture_systemd_docker_base_host_command_docker }} buildx build
          --memory={{ slovo_backend_container_image_build_memory }} --cpus={{ slovo_backend_container_image_build_cpus }}
          --tag={{ slovo_backend_container_image }}
          --file={{ slovo_backend_container_src_path }}/backend/Dockerfile
          {{ slovo_backend_container_src_path }}/backend
      when: slovo_backend_git_pull_changed.changed
      register: slovo_backend_docker_image_build_changed

- name: Ensure slovo-backend container network is created
  when: slovo_backend_container_network != 'host'
  community.general.docker_network:
    enable_ipv6: "{{ devture_systemd_docker_base_ipv6_enabled }}"
    name: "{{ slovo_backend_container_network }}"
    driver: bridge
    driver_options: "{{ devture_systemd_docker_base_container_networks_driver_options }}"

- name: Ensure slovo-backend.service installed
  ansible.builtin.template:
    src: "{{ role_path }}/templates/systemd/slovo-backend.service.j2"
    dest: "{{ devture_systemd_docker_base_systemd_path }}/slovo-backend.service"
    mode: '0644'
  register: slovo_backend_systemd_unit_changed

- name: Determine whether slovo-backend needs a restart
  ansible.builtin.set_fact:
    slovo_backend_restart_necessary: >-
      {{
        slovo_backend_env_file_changed.changed | default(false)
        or slovo_backend_labels_file_changed.changed | default(false)
        or slovo_backend_git_pull_changed.changed | default(false)
        or slovo_backend_docker_image_build_changed.changed | default(false)
        or slovo_backend_systemd_unit_changed.changed | default(false)
      }}
